{
  "name": "test-project",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "npx esbuild src/main.ts --bundle --format=esm --outfile=build/scripts/main.js --sourcemap",
    "dev": "npx esbuild src/main.ts --bundle --format=esm --outfile=build/scripts/main.js --sourcemap --watch"
  },
  "dependencies": {
    "esengine": "file:../.."
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "typescript": "^5.0.0"
  }
}
